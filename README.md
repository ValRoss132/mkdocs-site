## Кастомная тема MkDocs и CI/CD для статического сайта

🌐 Ссылка на сайт
https://valross132.github.io/mkdocs-site

1. Разработка собственной темы
   Вместо использования готовой темы (Material или MkDocs), была разработана собственная тема с нуля.

   - Технологии: HTML5, CSS3 (PostCSS), Jinja2 (движок шаблонов).
   - Структура темы (theme/):
     - base.html: Основной шаблон Jinja2. Включает в себя {% block content %} для вставки Markdown-контента.
     - Кастомный Header/Footer: Реализованы семантическими тегами `<header>` и `<footer>`.
     - Метаданные: В base.html динамически подставляются site_description и site_author из конфига.

2. Frontend Pipeline (Сборка и Оптимизация)
   Для обеспечения качества кода и оптимизации был внедрен пайплайн на базе Node.js:

   - PostCSS (postcss-cli): Используется для обработки CSS.
     - autoprefixer: Автоматически добавляет вендорные префиксы для совместимости браузеров.
     - cssnano: Минифицирует CSS файл перед публикацией.
   - Типографика (typograf): Скрипт scripts/process_html.js обрабатывает HTML после сборки, применяя правила русской типографики (неразрывные пробелы, правильные кавычки-елочки и тире).
   - Минификация HTML (html-minifier-terser): Удаляет лишние пробелы и комментарии из HTML файлов для ускорения загрузки.
   - Валидация (html-validate): Проверяет сгенерированный HTML на наличие семантических ошибок и незакрытых тегов.

3. CI/CD с GitHub Actions
   Автоматизация реализована в файле .github/workflows/action.yml и включает следующие этапы:

   1. Setup: Установка Python (MkDocs) и Node.js (инструменты сборки).
   2. Build CSS: Компиляция исходного CSS (src/css/main.css) в папку темы (theme/css/style.css) с помощью PostCSS.
   3. Build Site: Генерация статического сайта командой mkdocs build.
   4. Test: Запуск HTML-валидатора для проверки качества кода.
   5. Optimize: Запуск скрипта пост-обработки (Типограф + Минификация).
   6. Deploy: Загрузка артефакта и деплой на GitHub Pages.
